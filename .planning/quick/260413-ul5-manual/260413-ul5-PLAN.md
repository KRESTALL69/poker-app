---
phase: quick-260413-ul5-manual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/admin/players/[id]/route.ts
  - features/admin.ts
  - app/admin/moderation/page.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Кнопка 'Удалить' видна только у игроков с telegram_id = null"
    - "Нажатие кнопки удаляет игрока и его связанные записи из БД"
    - "Игроков с telegram_id удалить нельзя — кнопка не отображается"
    - "После удаления игрок исчезает из каталога без перезагрузки страницы"
  artifacts:
    - path: "app/api/admin/players/[id]/route.ts"
      provides: "DELETE endpoint для удаления manual игрока"
    - path: "features/admin.ts"
      provides: "Функция deleteManualPlayer с очисткой связанных записей"
    - path: "app/admin/moderation/page.tsx"
      provides: "Кнопка Удалить в каталоге для telegram_id = null игроков"
  key_links:
    - from: "app/admin/moderation/page.tsx"
      to: "/api/admin/players/{id}"
      via: "fetchAdminJson DELETE"
    - from: "app/api/admin/players/[id]/route.ts"
      to: "features/admin.deleteManualPlayer"
      via: "import и вызов"
---

<objective>
Добавить удаление manual игроков (telegram_id = null) в каталоге модерации ников.

Purpose: Администратор должен иметь возможность удалять игроков, созданных вручную, без привязки к Telegram.
Output: DELETE API endpoint + функция удаления в features/admin.ts + кнопка в UI.
</objective>

<execution_context>
@C:/Users/KRESTALL/poker-app/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/KRESTALL/poker-app/.planning/STATE.md
@C:/Users/KRESTALL/poker-app/features/admin.ts
@C:/Users/KRESTALL/poker-app/app/api/admin/players/[id]/access/route.ts
@C:/Users/KRESTALL/poker-app/app/admin/moderation/page.tsx
@C:/Users/KRESTALL/poker-app/types/database.ts

<interfaces>
<!-- Используется в плане -->

From types/database.ts:
```typescript
export type PlayerRow = {
  id: string;
  telegram_id: number | null;
  // ...
};

export type RegistrationRow = { id: string; player_id: string; ... };
export type ResultRow = { id: string; player_id: string; ... };
export type TournamentLiveEntryRow = { id: string; player_id: string; ... };
export type PlayerAchievementRow = { id: string; player_id: string; ... };
```

From types/domain.ts (Player):
```typescript
export interface Player {
  id: string;
  telegram_id: number | null;
  // ...
}
```

fetchAdminJson — обёртка для admin-запросов, используется в moderation/page.tsx:
```typescript
import { fetchAdminJson } from "@/lib/client-request";
```

supabase импортируется в features/ как:
```typescript
import { supabase } from "@/lib/supabase";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить deleteManualPlayer в features/admin.ts и DELETE route</name>
  <files>features/admin.ts, app/api/admin/players/[id]/route.ts</files>
  <action>
В `features/admin.ts` добавить функцию `deleteManualPlayer(playerId: string): Promise<void>`:

1. Сначала проверить, что у игрока telegram_id IS NULL:
   ```ts
   const { data: player, error: fetchError } = await supabase
     .from("players")
     .select("telegram_id")
     .eq("id", playerId)
     .single();
   if (fetchError || !player) throw new Error("Игрок не найден");
   if (player.telegram_id !== null) throw new Error("Нельзя удалять игроков с Telegram");
   ```

2. Удалить связанные записи в правильном порядке (FK constraints):
   - `tournament_live_entries` где player_id = playerId
   - `player_achievements` где player_id = playerId
   - `results` где player_id = playerId
   - `registrations` где player_id = playerId

3. Удалить самого игрока:
   ```ts
   const { error } = await supabase.from("players").delete().eq("id", playerId);
   if (error) throw new Error(`Ошибка удаления: ${error.message}`);
   ```

Создать файл `app/api/admin/players/[id]/route.ts` (рядом с уже существующим каталогом `access/`):

```ts
import { NextResponse } from "next/server";
import { deleteManualPlayer } from "@/features/admin";

export async function DELETE(
  _request: Request,
  context: { params: Promise<{ id: string }> }
) {
  try {
    const { id } = await context.params;
    await deleteManualPlayer(id);
    return NextResponse.json({ success: true });
  } catch (error) {
    return NextResponse.json(
      { error: error instanceof Error ? error.message : "Ошибка удаления игрока" },
      { status: 500 }
    );
  }
}
```
  </action>
  <verify>curl -X DELETE http://localhost:3000/api/admin/players/FAKE_ID возвращает 500 с текстом "Игрок не найден" (ожидаемо для несуществующего ID). Или проверить TypeScript через `npx tsc --noEmit`.</verify>
  <done>features/admin.ts экспортирует deleteManualPlayer, app/api/admin/players/[id]/route.ts существует с DELETE handler. TypeScript компилируется без ошибок.</done>
</task>

<task type="auto">
  <name>Task 2: Добавить кнопку "Удалить" в каталог игроков (moderation/page.tsx)</name>
  <files>app/admin/moderation/page.tsx</files>
  <action>
В компоненте `AdminModerationPage` добавить:

1. Добавить функцию `handleDeleteManualPlayer(playerId: string)`:
```tsx
async function handleDeleteManualPlayer(playerId: string) {
  if (!confirm("Удалить этого игрока? Это действие необратимо.")) return;
  try {
    setProcessingKey(`delete-${playerId}`);
    setMessage(null);
    setError(null);
    await fetchAdminJson(`/api/admin/players/${playerId}`, { method: "DELETE" });
    setPlayers((prev) => prev.filter((p) => p.id !== playerId));
    setMessage("Игрок удалён");
  } catch (err) {
    setError(err instanceof Error ? err.message : "Ошибка удаления");
  } finally {
    setProcessingKey(null);
  }
}
```

2. В блоке рендеринга каталога игроков (filteredPlayers.map), изменить grid-cols строку карточки с `grid-cols-[40px_minmax(0,1fr)_auto]` на `grid-cols-[40px_minmax(0,1fr)_auto_auto]` — добавляется ячейка для кнопки удаления.

3. После кнопки "Редактировать" / "Сохранить" добавить — только если `!targetPlayer.telegram_id`:
```tsx
{!targetPlayer.telegram_id ? (
  <button
    type="button"
    onClick={() => handleDeleteManualPlayer(targetPlayer.id)}
    disabled={processingKey === `delete-${targetPlayer.id}`}
    className="rounded-lg border border-red-500/30 px-3 py-2 text-sm font-medium text-red-400 disabled:opacity-60"
  >
    {processingKey === `delete-${targetPlayer.id}` ? "..." : "Удалить"}
  </button>
) : null}
```

Не трогать секцию "Заявки на модерации" (pendingPlayers) — только каталог (filteredPlayers.map).
  </action>
  <verify>Открыть /admin/moderation в браузере. У игроков без telegram_id видна красная кнопка "Удалить". У игроков с telegram_id кнопка отсутствует. Нажатие кнопки на manual игроке — появляется confirm, после подтверждения игрок исчезает из списка.</verify>
  <done>Кнопка "Удалить" отображается только для игроков с telegram_id = null. Удаление работает и обновляет список локально без reload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI → DELETE /api/admin/players/:id | Запрос не аутентифицирован на уровне сервера (известная проблема, зафиксирована в CONCERNS.md) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ul5-01 | Tampering | DELETE /api/admin/players/:id | accept | Защита на уровне admin-маршрута отсутствует по всему проекту (tracked in CONCERNS.md). Удаление Telegram-игроков заблокировано на уровне features/admin.ts — server-side guard. |
| T-ul5-02 | Elevation of Privilege | deleteManualPlayer | mitigate | Проверка telegram_id IS NULL внутри функции до любых delete-операций — нельзя удалить Telegram-игрока даже при прямом API-вызове. |
</threat_model>

<verification>
1. `npx tsc --noEmit` — нет новых TypeScript-ошибок.
2. В каталоге игроков: ручной игрок (telegram_id = null) имеет кнопку "Удалить".
3. Telegram-игрок — кнопки нет.
4. После подтверждения удаления игрок исчезает из списка (optimistic update через setPlayers filter).
5. Прямой DELETE на /api/admin/players/{id} Telegram-игрока возвращает 500 "Нельзя удалять игроков с Telegram".
</verification>

<success_criteria>
- deleteManualPlayer экспортируется из features/admin.ts, проверяет telegram_id перед удалением, удаляет связанные записи в правильном порядке
- DELETE /api/admin/players/[id] route существует и вызывает deleteManualPlayer
- Кнопка "Удалить" видна только у игроков с telegram_id = null в каталоге модерации
- TypeScript компилируется без ошибок
</success_criteria>

<output>
После выполнения создать `.planning/quick/260413-ul5-manual/260413-ul5-SUMMARY.md`
</output>
